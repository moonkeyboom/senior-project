# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

ผู้ใช้หลักคือ **นักวิจัย / ผู้ทำเปเปอร์ที่เปรียบเทียบวิธี discretization** (ผู้พัฒนาโปรเจกต์เองอยู่ในกลุ่มนี้)

สถานการณ์การใช้งาน: มี dataset คะแนนหลายชุด (ข้อมูลจริงจากรายวิชา + ชุดทดสอบสังเคราะห์ที่ออกแบบมาให้มี edge case) แล้วต้องการรันซ้ำหลายรอบเพื่อดูว่าอัลกอริทึมไหนได้ค่า Ω′ สูงกว่ากันภายใต้ label budget เดียวกัน งานที่ทำจริงคือ "รัน → อ่านตัวเลข → เทียบ → บันทึกผลไว้อ้างอิง" ไม่ใช่การตัดเกรดครั้งเดียวจบ

ยังไม่ได้ยืนยันกลุ่มผู้ใช้อื่น (อาจารย์ผู้สอนที่เอาไปตัดเกรดจริง, กรรมการสอบ) ว่าเป็นผู้ใช้ที่ระบบต้องรองรับ — งานในอนาคตห้ามสมมติเอง

## Product Purpose

หา partition ของคะแนน 1 มิติที่ทำให้ค่า **Ω′ (Omega prime)** สูงสุด แทนการ minimize withinss/SSE แบบ k-means ดั้งเดิม

Ω′ คือ metric วัด "ความยุติธรรมแบบมีเงื่อนไข" (conditional unbiasedness) ของการแบ่งกลุ่ม ตาม Eq.(2) ของเปเปอร์ PeerJ CS 2025 ประกอบด้วย Ω1 · Ω2 · Ω3

ความสำเร็จของโปรดักต์ = ตอบคำถามวิจัยได้ว่า วิธีที่รับประกัน optimal (exhaustive / DP-based) ให้ Ω′ ดีกว่า heuristic baselines (WGF-CPD, PAM-CPD, K-CPD) มากน้อยแค่ไหน บนข้อมูลจริง โดยผลที่ได้ต้องตรวจสอบย้อนหลังได้และ export ไปใช้ต่อในเปเปอร์ได้

## Positioning

สิ่งที่โปรเจกต์อื่นลอกไม่ได้ตรงๆ คือ **implementation ของ Ω′ ที่ถอดสมการจริงพร้อมหลักฐานการ verify** ไม่ใช่การประมาณ:

- `calculate_omega_prime()` verify ตรงกับ Table 3 ของเปเปอร์ (A1 = 0.0824 ≈ 0.08, A2 = 0.2205 ≈ 0.22)
- ถอดได้ว่า **Ω1 = θ/Θ** ไม่ใช่ 1 − θ/Θ ตามที่ OCR ของ Eq.(2) อ่านได้ ยืนยันซ้ำกับตัวอย่าง A1, A2 และ EMP1 K-CPD (0.82)
- **σ ใน Ω3 ใช้ sample std (ddof=1)** ไม่ใช่ population std โค้ดรุ่นก่อน (`bin/run_cpd_theta_version.py`) ใช้ `np.std` ddof=0 ซึ่งผิด
- **D_min/D_max ใน Ω2 นับเฉพาะ gap ระหว่างค่าที่อยู่ติดกัน** ไม่รวม γu, γl

และมี **exhaustive search** เป็น ground truth ที่รับประกัน global optimum จริง (C(n−1, k−1)) ใช้เป็นมาตรฐานตัดสินว่า DP หรือ heuristic ห่างจากคำตอบที่ดีที่สุดแค่ไหน

## Operating Context

รันเป็น 2 services ด้วย `docker-compose up -d --build` — backend port 8000, frontend port 5173

Workflow ปัจจุบัน:
1. อัปโหลดไฟล์ `.csv` / `.xlsx`
2. ระบบดึงคะแนนจากคอลัมน์ชื่อ `score`, `total` หรือ `คะแนน` ถ้าไม่เจอจะใช้คอลัมน์ตัวเลขคอลัมน์สุดท้าย
3. ระบุระดับเกรดคั่นด้วยเครื่องหมายจุลภาค (ค่า default ปัจจุบัน: `A, B+, B, C+, C, D+, D, F`)
4. `POST /api/calculate/stream` ส่งกลับเป็น NDJSON stream — event ชนิด `log` (ขั้นตอนการทำงานพร้อม elapsed_ms), `result` (ผลสุดท้าย), `error`
5. หน้าเว็บแสดง Ω′ รวม + Ω1/Ω2/Ω3 แยก, ตาราง Ω′ ของทุกอัลกอริทึมเรียงจากดีสุด, ตารางสรุปช่วงเกรด, และตารางรายคน

## Capabilities and Constraints

### ทำได้แล้ว
- อัปโหลด CSV / XLSX + ตรวจหาคอลัมน์คะแนนอัตโนมัติ
- กำหนดรายการ label เองได้
- คำนวณ Ω′ ของทุกอัลกอริทึมในรอบเดียวแล้วเรียงเทียบกัน
- Streaming execution log แบบ real-time พร้อม elapsed time ต่อขั้น
- ตารางสรุปช่วงเกรด (min / max / จำนวน) และตารางรายคน

### ข้อผูกมัดถาวร (ผู้ใช้ยืนยันแล้ว — งานในอนาคตห้ามทำพัง)
1. **execution log ต้องตรวจย้อนหลังได้** ทุกขั้นตอนตั้งแต่อ่านไฟล์ → เลือกคอลัมน์ → สูตรที่ใช้ → ผลแต่ละวิธี ต้องมองเห็น ห้ามกลายเป็น black box
2. **ต้อง fix จำนวนเกรด |L|** ห้าม sweep k อิสระ เหตุผลคือ Ω′ degeneracy: เมื่อ N < 3 → Ω2 ถูกบังคับเป็น 1 และการแบ่ง 2 กลุ่มที่ PVI เท่ากันทำให้ σ = 0 → Ω3 = 1 ด้วย ผลคือ argmax Ω′ ยุบไปที่ N = 2 แล้วได้ Ω′ = 1.0 แบบไร้ความหมาย Ω′ เป็น metric ไว้ "เปรียบเทียบวิธี" ที่ label budget คงที่ ไม่ใช่ objective ที่ค้นอิสระข้าม k
3. **ต้อง export ผลกลับเป็นไฟล์ได้** (csv / xlsx ที่มีเกรดต่อคน) — ยังไม่มีในระบบตอนนี้ ถือเป็น capability ที่ต้องมี
4. **ภาษาไทย + อังกฤษ สลับได้** เป็นเป้าหมายระยะยาว ตอนนี้ UI hardcode ภาษาไทยล้วน ยังไม่มี i18n layer

### ยังไม่ตัดสิน (ห้ามเดาแทน)
- ค่า U (คะแนนเต็ม) และ L (คะแนนต่ำสุด) — ปัจจุบัน default 100 / 0
- ชุดสัญลักษณ์เกรดมาตรฐาน — ปัจจุบัน 8 ตัว (A, B+, B, C+, C, D+, D, F)
- ตัวเลขที่จะใช้เป็น |L| จริงตอนรันกับข้อมูลจริง

### ศัพท์เฉพาะที่ต้องใช้ให้ตรง
Ω′ (Omega prime), Ω1 / Ω2 / Ω3, PVI, θ / Θ, |L| (label budget), N (จำนวนเกรดที่ถูก assign จริง), CPD (Conditional Performance Discretization), WGF-CPD, PAM-CPD, K-CPD, M-CPD, withinss / SSE, Ckmeans.1d.dp

## Brand Commitments

ผู้ใช้ระบุเป็นข้อผูกมัด: **ใช้ฟอนต์เดียวกับ Ghostty terminal ที่ผู้ใช้ใช้อยู่ ทั้งโปรเจกต์**

ข้อเท็จจริงที่ตรวจสอบแล้ว ณ ตอนบันทึก: เครื่องนี้ไม่มีไฟล์ config ของ Ghostty (`~/.config/ghostty/config` และ `~/Library/Application Support/com.mitchellh.ghostty/config` ไม่มีอยู่จริง) `font-family` จึงว่าง = ใช้ค่า default ที่ Ghostty 1.3.1 ฝังมาในตัว ที่ `font-size = 13` ฟอนต์ที่ผู้ใช้ติดตั้งเองในเครื่องคือ Hack Nerd Font และ THSarabun

**เปิดค้างไว้:** ฟอนต์ terminal เป็น monospace ที่ไม่มี glyph ภาษาไทย ข้อความไทยจะ fallback ไปฟอนต์อื่นโดยอัตโนมัติ ทำให้ไทยกับอังกฤษเป็นคนละฟอนต์ ต้องตัดสินตอนงาน typography ว่าจะให้ mono ครอบทุกที่แล้วยอม fallback หรือจำกัด mono ไว้เฉพาะตัวเลข / code / log

ยังไม่มีชื่อแบรนด์, โลโก้, หรือ voice ที่ยืนยัน

## Evidence on Hand

**เปเปอร์**
- `file/paper/Ckmeans.1d.dp_ Optimal k-means Clustering in One Dimension by Dyn.pdf` — Wang & Song (2011), R Journal
- Banditwattanawong & Masdisornchote (2025), PeerJ CS, DOI 10.7717/peerj-cs.2804 — เป็นที่มาของ Ω′ Eq.(2) และ Algorithm 1–4 **ตัว PDF ไม่ได้อยู่ใน repo**

**ข้อมูลจริง**
- `file/input/221.xlsx` — คะแนนจริงจากรายวิชา
- `file/paper/ggสเปรดชีตไม่มีชื่อ - WGP.csv` — ข้อมูล WGP (CONTEXT.md เคยระบุว่าอ่านไม่ได้ ต้องตรวจซ้ำก่อนใช้)

**ชุดทดสอบสังเคราะห์** — `data/tc1_uniform.csv` ถึง `data/tc10_gaps.csv` ครอบ uniform, bimodal, outlier สูง/ต่ำ, skewed, normal, narrow, small n, five groups, gaps

**ผลลัพธ์ baseline ที่รันไว้แล้ว** — `file/output/heuristic/`, `file/output/kmean/`, `file/output/zscore/`

**หลักฐานการ verify** — `results.csv`, `summary_chart.png`, `test_summary.txt`, `test_cpd_cases.py` และผล 2 การตรวจสอบ: (1) Table 3 ตรงเปเปอร์ (2) exhaustive ≥ DP ทุกค่า k

**ไม่มีและห้ามแต่งขึ้น** — ไม่มีผู้ใช้จริงนอกทีม ไม่มี testimonial ไม่มี benchmark เทียบกับซอฟต์แวร์เชิงพาณิชย์ ไม่มีตัวเลขจำนวนสถาบันหรือจำนวนนักศึกษาที่ใช้งาน

## Product Principles

1. **ตัวเลขทุกตัวต้องย้อนกลับไปหาที่มาได้** ค่า Ω′ ที่โชว์ต้องตามรอยกลับไปถึงสูตรและขั้นตอนที่คำนวณมันออกมาได้เสมอ นี่คือเหตุผลที่ execution log เป็นข้อผูกมัด ไม่ใช่ของประดับ

2. **ไม่โชว์ค่าเดี่ยว โชว์การเปรียบเทียบ** Ω′ ตัวเดียวไม่มีความหมาย มันมีความหมายเมื่ออยู่ข้าง baseline ที่มันชนะหรือแพ้

3. **|L| เป็น input ไม่ใช่สิ่งที่ระบบไปค้นเอง** การให้ระบบเลือกจำนวนเกรดเองทำให้ metric พัง ข้อจำกัดนี้มาจากคณิตศาสตร์ ไม่ใช่ความชอบ

4. **งานจบที่ไฟล์ ไม่ใช่ที่หน้าจอ** ผู้ใช้ต้องเอาผลไปเขียนเปเปอร์ต่อ ผลที่ออกจากระบบไม่ได้ = ยังไม่เสร็จ

5. **บอกความไม่แน่นอนตามจริง** เมื่อ exhaustive รันไม่ไหวเพราะ n ใหญ่ หรือข้อมูลมี edge case ที่ทำให้ผลไม่น่าเชื่อถือ ระบบต้องพูดออกมา ไม่ใช่คืนตัวเลขเฉยๆ
